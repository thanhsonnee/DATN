package com.gym.feedback.domain;

/** Loại phản hồi — quyết định luồng xử lý tự động khi tạo (xem {@code FeedbackService}). */
public enum FeedbackType { TRAINER, FACILITY, HYGIENE, SERVICE, GENERAL }
